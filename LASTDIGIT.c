#include<stdio.h>
int n1,n2;
//char c[5],ch[5];
void lastdigit()
{
    r=a%10;
    rev=rev*10+r;
    a=a/10;

  /*int s1=strlen(c);
    int s2=strlen(ch);
    for(int i=s1-1,j=s2-1;c[i]!=NULL,ch[j]!=NULL;i++,j++)
    {
        if(c[i]==ch[j])
        printf("same");
        else
        printf("not same ");
    }*/
}
main()
{
    //printf("enter the string ");
    //scanf("%s%s",&c,&ch);
    lastdigit();
}
