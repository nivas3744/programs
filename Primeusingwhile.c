#include<stdio.h>
main()
{
    int c,n,r;
    printf("enter the number ");
    scanf("%d",&r);
    while()
    {
        c=0;int i=2;
        while(i<n)
        {

        if(n%i==0)
        {
            c++;
        }i++;
        }
        if(c==0 && a!=1)
        printf("\t%d",n);
         a++;
    }
}
