#include<stdio.h>
main()
{
    int n,sum,sq;
    printf("Enter the number : ");
    scanf("%d",&n);
    printf("square of the given numbers are ");
    for(int i=1;i<=n;i++)
    {
          sq=i*i;
          '
          '
            sum=sum+sq;
            printf("\n%d",sq);
        }
    printf("\nsum of these number is %d ",sum);
    }
